name: "compare_optimizers"
metric_names: ["param_euclidean_dist"]

# Config A: Standard SGD
config_a:
  defaults_path: "base.yaml"
  name: "baseline_sgd"
  training_config:
    optimizer_name: "SGD"

# Config B: Our Custom Optimizer
config_b:
  defaults_path: "base.yaml"
  name: "experimental_noise"
  training_config:
    optimizer_name: "SgdWithNoise"