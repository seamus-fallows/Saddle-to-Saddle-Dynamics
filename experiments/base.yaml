name: "base_run"
input_dim: 5
output_dim: 5
model_seed: 42

dln_config:
  num_hidden: 3
  hidden_size: 100
  gamma: 1.5
  bias: false

training_config:
  lr: 0.0001
  max_steps: 10000
  evaluate_every: 1
  optimizer_name: "SGD"
  criterion_name: "MSELoss"
  batch_size: null  # Full batch

data_config:
  name: "diagonal_teacher"
  num_samples: 100
  data_seed: 42
  scale_factor: 10.0
  test_split: null # No test split